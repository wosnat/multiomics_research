{
  "mcpServers": {
    "multiomics-kg": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${MULTIOMICS_EXPLORER_DIR}", 
        "multiomics-kg-mcp"
      ]
    }
  }
}
